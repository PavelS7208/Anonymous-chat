# Тестовый проект: Анонимный чат

Создан по [ТЗ для анонимного чата](docs/taskDescr.md)

ReadMe [Этапа 3](docs/README_step_3_main.md)

## Реализация Этап 4

### Результаты собственного ревью этапа 3

1. В доменном объекте комната (Room + Member) зашиты детали реализации стриминга через каналы, буферы, алгоритмя отработки с медленными клиентами и т.п. По хорошему, это нужно вынести на инфраструктурный слой, чтобы при тестировании оставить только логику без необходимости мокать инфраструктурные сущности механизма передачи. Подумать как это реализовать, например что-то похожее на Pub-Sub. Оценить стоит ли, плюсы-минусы. **(Выполнено)**
2. В основных методах сервисного слоя Join, Stream, Send реализация состояит из последовательности (строго в определенном порядке) вызовов необходимых методов. При этом уже 5+ таких шагов и скорее всего будет расширяться. Подумать, может стоит перейти на паттерн pipeline для упрощения понимания и добавления (в случае необходимости) новых составляющих алгоритма работы чата. **(Выполнено)**
3. Защитники от сетевых атак добавлялись произвольно по мере понимания их необходммости, они с разной архитектурой. Вызовы методов с разной сигнатурой, разным неймингом и т.п. Подумать, как бы их "спрятать" за один (несколько) универсальных интерфейсов с унифицированной сигнатурой методов. **(Выполнено)**
4. Механизм пометок зависших или давно не используемых комнат и удаления их реализован в одном общем типе что и InMemory хранилище комнат, как дополнительные методы. Подумать может стоит вынести, для чистоты ответвенности, данный функционал в отдельный сервис за типовой интерфейс. **(Выполнено)**
5. В ЖЦ чата интерфейс содержит два метода Start/ Close. Похоже требуется что-то вроде Wait() где на основе WaitGroup или иного ожидается завершение запущенных сервисных процедур. Сейчас приходится в доменных объектах и сервисном слое проверять на isClose() чтоюбы не попасть на ситуацию, когда ассинхронность Close при завершении сервера или иных причинах идет в неконтролируемом порядке. **(Выполнено)**
6. В текущей архитектуру жив ли клиент, мы узнаем только при отправке ему сообщения. Подумать - может стоить придумать механизм определения состояния клиента и при кратковременных сетевых проблемах переподолючение (с защитой взлома) без новой регистрации с новой парой ключей. **(Ограничение постановки и chunked стрима)**
7. Ошибки уровня транспорта и маппинг на них доменных и сервисных ошибок "размазан" по коду. Подумать как собрать это все в одном месте, для простоты настройки/изменения какие коды и с каким текстом и доп заголовками отдаем по http
8. Разобраться подробно в иерархии context в системе. При отмене http клиента его событие left не уходит в историю, так как ctx отменен. Необходимо ввести поняте room_ctx как наследника от server_ctx  чтобы все события от отвалившихся клиентов отрабатывались до конца и всегда с гарантией. **(Выполнено)**
9. TOCTOU проблема при реализации проверки на максимальное кол-во комнат и участников в комнате при запросе из хранилища. Нужны атомарные операции. **(Выполнено)**

In progress ....

### Запланировать реализацию дополнений

1. Запуск сервера в контейнере. **(Выполнено)**
2. Разобраться как в логировании прикручивать сквозной лог используя traceID и подобные техники. **(Выполнено)**
3. К логированию прикрутить минимальное отображение/поиск. Найти простые решения и реализовать, не прибегая пока к ELK - на подумать и разобраться. **(Отложено для следующей версии. Пока просто лог JSON структуры в файл и std)**
4. Добавить метрики. Возможно прикрутить минимальный комплект  Prometheus/Grafana - на подумать и разобраться **(В процессе)**
5. Юнит тесты покрытие увеличить. Рассмотреть инфраструктурные - разобраться как их сделать.

In progress ....


## Изменения и дополнения в функционале

In progress ....

## Особенности программной реализации

Структура проекта и особенности реализации отражены в отдельном readme [этапа 4](docs/README_step_4.md)

## Запуск и выполнение


Запуск в контейнере:
```
docker compose up --build
```


In progress ....

